import React from 'react';

const Header = () => {
    return (
        <div className='header'>
            <div className="header-logo">
                <i className="massive angle double left icon"></i>
                <a className="header-text" href="./">notes</a>
                <i className="massive angle double right icon"></i>
                <p className="golden-thought">“Rather than make the first move it is better to wait and see. Rather than advance an inch it is better to retreat a yard.” (Tao Te Ching)</p>
            </div>
        </div>
    )
}

export default Header;